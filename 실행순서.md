# 엘리스 실행 순서 — 최소 구성 (2Y8XQ 영상 하나로)

벤치마크 대용량 다운로드 없이, **공식 데모 영상 하나(2.3MB)로** 끝까지 갑니다.
나중에 더 필요하면 `영상_다운로드_URL.md` 를 보세요.

---

## 0. 올리기 전에 — 로컬 정리

```bash
cd ~/Documents/Video_grounding
rm -rf videos/_bench          # 받다 만 벤치마크 데이터 (733MB) — 지금은 불필요
ls videos/                     # 2Y8XQ.mp4 와 external.json 만 남으면 OK
```

`videos/2Y8XQ.mp4` 가 있으면 됩니다. 없으면:

```bash
mkdir -p videos && curl -fL -o videos/2Y8XQ.mp4 \
  "https://huggingface.co/datasets/JungleGym/TimeLens-Assets/resolve/main/2Y8XQ.mp4"
```

---

## 1. 인스턴스 만들기

| 항목 | 선택 |
|---|---|
| GPU | **G-NAHPM-40** (A100 MIG 3g-40GB, ₩1,380/시간) |
| 스토리지 | **128 GiB** |
| 실행 환경 | **VSCode (CUDA 12.8)** ← 11.8 은 안 됩니다 |

만들면 브라우저에서 VSCode 가 열립니다.

---

## 2. 파일 올리기 (1분)

**`vtg_bundle.tar.gz` 파일 하나만 올리면 됩니다.** (2.2MB, 코드 + 영상 전부 포함)
폴더 드래그는 잘 안 되는 경우가 많아서 하나로 묶어뒀습니다.

### 방법 A — VSCode 업로드 (가장 쉬움)

```
① VSCode 왼쪽 탐색기의 빈 공간에서 우클릭
② "Upload..." 클릭          (없으면 "업로드" / "Upload Files")
③ vtg_bundle.tar.gz 선택
```

드래그 앤 드롭이 안 되면 **우클릭 → Upload** 를 쓰세요. 파일 선택창이 뜹니다.

올린 뒤 터미널에서:

```bash
mkdir -p ~/Video_grounding && cd ~/Video_grounding
tar -xzf ~/vtg_bundle.tar.gz        # 업로드된 위치에 맞게 경로 조정
ls                                   # app.py, vtg_run.py, videos/ 가 보이면 성공
```

> 업로드 위치를 모르겠으면: `find ~ -name "vtg_bundle.tar.gz" 2>/dev/null`

### 방법 B — 터미널에서 직접 (업로드가 아예 안 될 때)

인스턴스 터미널에서 이 한 줄이면 **영상은 바로 받아집니다.**

```bash
mkdir -p ~/Video_grounding/videos && cd ~/Video_grounding
curl -fL -o videos/2Y8XQ.mp4 \
  "https://huggingface.co/datasets/JungleGym/TimeLens-Assets/resolve/main/2Y8XQ.mp4"
```

코드 파일들은 VSCode에서 **새 파일을 만들어 붙여넣기** 하시면 됩니다
(`app.py`, `vtg_run.py`, `setup_env.sh` 세 개만 있어도 UI는 돌아갑니다).

### 방법 C — scp (맥 터미널에서)

엘리스 콘솔 → `내 인스턴스` → `다른 SSH 클라이언트 사용` 에서 접속 정보 확인 후:

```bash
# scp 는 포트가 대문자 -P 입니다 (ssh 는 소문자 -p)
scp -i ~/키.pem -P 포트 ~/Documents/Video_grounding/vtg_bundle.tar.gz 계정@주소:~/
```

---

## 3. 환경 구성 (10~15분)

`Terminal` → `New Terminal` 에서:

```bash
cd ~/Video_grounding
bash setup_env.sh
```

- **"지금 다운로드할까요?"** → **`y`** (모델 26GB, 약 10분)
  - 시간을 아끼려면 `N` 을 눌러도 됩니다. 나중에 UI 첫 실행 때 자동으로 받습니다
- 마지막에 **`>>> 환경 정상 <<<`** 이 뜨면 성공

```bash
source ~/vtg-env/bin/activate
sudo apt-get install -y ffmpeg      # "command not found" 뜰 때만
```

> 터미널을 새로 열 때마다 `source ~/vtg-env/bin/activate` 를 다시 하세요.

---

## 4. 동작 확인 (5분, 대부분 모델 다운로드)

**가장 가벼운 모델로 먼저.** 여기서 되면 나머지도 됩니다.

```bash
python3 vtg_run.py --model timelens2-4b --video videos/2Y8XQ.mp4 \
    --query "A man drinks water with a glass"
```

이런 출력이 나오면 성공입니다.

```
  [[12.4, 18.7]]    3.2s   9.1GB | A man drinks water with a glass
```

---

## 5. 웹 UI 띄우기

```bash
python3 app.py
```

VSCode 터미널에 **포트 알림**이 뜹니다 → `브라우저에서 열기`.
안 뜨면 하단 `포트` 탭에서 `7860` 을 열거나:

```bash
python3 app.py --share      # 임시 공개 링크가 터미널에 표시됨
```

---

## 6. 사용법

```
① "서버에 있는 영상" 드롭다운에서  videos/2Y8XQ.mp4  선택
② 영상이 재생됩니다 —— 여기서 직접 보세요
③ 본 내용을 바탕으로 질의를 영어로 씁니다
④ [장면 찾기]
```

**질의는 이미 준비돼 있습니다.** 영상을 고르면 드롭다운에 6개가 뜹니다.
제가 프레임을 1초 간격으로 뽑아 눈으로 확인하고 정답 시각까지 붙여뒀어요.

| 질의 | 정답 |
|---|---|
| `A man drinks water with a glass` (공식) | 17~19초 |
| `a man drinks from a plastic water bottle` | 17~19초 |
| `a man picks up a bottle from the floor` | 4~6초 |
| `a man wipes his face with a pink towel` | 26~28초 |
| `a man shakes out a pink towel` | 0~3초 |
| `a dog runs across the room` | **없음** (한계 시연용) |

영상 내용과 발표 시나리오는 **`2Y8XQ_분석.md`** 에 정리해뒀습니다.

내 컴퓨터에 있는 영상을 쓰려면 드롭다운을 `— 직접 올리기 —` 로 두고
영상 칸에 **끌어다 놓으면** 됩니다.

---

## 7. 발표 시연 순서

**① 공식 질의로 시작**

```
A man drinks water with a glass          → 정답 17~19초
```

**② 주석이 틀렸다는 걸 보여주기** ← 이 데모의 핵심

영상에 나오는 건 유리컵이 아니라 **플라스틱 물병**입니다. 공식 질의가 부정확해요.

```
A man drinks water with a glass             (부정확한 원본)
a man drinks from a plastic water bottle    (실제 영상에 맞는 표현)
```

**TimeLens 논문이 지적한 바로 그 문제**입니다 — Charades-STA 의 34.9% 가
annotation accuracy issues 를 갖고 있다는 것. 데모 영상에서도 그대로 나타납니다.

**③ 영상 전체를 보는지**

```
a man picks up a bottle from the floor    → 4~6초   (앞)
a man wipes his face with a pink towel    → 26~28초 (뒤)
```

**④ 한계**

```
a dog runs across the room                → 개가 없음
```

"없다"고 못 하고 아무 구간이나 답합니다 (no-target rejection 미해결).

**⑤ 모델을 바꿔서 비교**

모델 드롭다운만 바꾸고 ①을 다시 돌립니다.

| 모델 | 백본 | 논문 Charades mIoU |
|---|---|---|
| **TimeLens-8B** | Qwen3-VL-8B | **55.2** |
| TimeLens-7B | Qwen2.5-VL-7B | 48.8 |
| TimeLens2-8B | Qwen3-VL-8B | 58.6 |

**TimeLens-8B vs TimeLens-7B** 는 백본 세대 차이(Qwen3-VL vs Qwen2.5-VL)를,
**TimeLens vs TimeLens2** 는 학습 방법 차이를 보여줍니다.

> 이 5단계면 15분짜리 발표로 충분합니다.

---

## 8. 마무리

**인스턴스를 반드시 삭제하세요.** 켜두면 계속 과금됩니다.

---

## 예상 비용

| 단계 | 시간 | 누적 |
|---|---|---|
| 업로드 + 환경 구성 | 15분 | ₩350 |
| 모델 다운로드 | 10분 | ₩600 |
| UI로 시연 연습 | 30분 | ₩1,300 |

**₩1,500 안쪽**이면 끝납니다.

---

## 자주 나오는 문제

| 증상 | 대응 |
|---|---|
| `GPU(CUDA)를 찾을 수 없습니다` | 맥북에서 실행 중. 인스턴스 터미널에서 하세요 |
| `no kernel image is available` | CUDA 12.8 이미지가 맞는지 확인 후 `bash setup_env.sh` 다시 |
| `ModuleNotFoundError` | `source ~/vtg-env/bin/activate` 를 안 한 상태 |
| UI가 안 열림 | `python3 app.py --share` |
| 폴더 드래그 업로드가 안 됨 | 탐색기 빈 공간 **우클릭 → Upload...** 로 `vtg_bundle.tar.gz` 하나만 |
| 올린 파일을 못 찾음 | `find ~ -name "vtg_bundle.tar.gz" 2>/dev/null` |
| `tar: not found` 류 | `sudo apt-get install -y tar` (거의 없을 일) |
| 드롭다운에 영상이 없음 | `videos/` 에 mp4 를 넣고 UI의 **새로고침** |
| 클립이 안 잘림 | `sudo apt-get install -y ffmpeg` (구간 숫자는 그래도 나옴) |
| `CUDA out of memory` | 모델을 `TimeLens2-4B` 로 |
| `구간을 찾지 못했습니다` | 문장이 모호함. 더 구체적으로 |

막히면 **에러 메시지 전문**과 **어느 단계**였는지 알려주세요.

---

## 나중에 더 필요해지면

| 하고 싶은 것 | 명령 |
|---|---|
| 정답(GT) 있는 영상으로 tIoU 측정 | `python3 run_experiments.py --download-bench --bench-pick "E6DLK,KOVTR" --cleanup-shard` |
| 여러 모델 한 번에 비교 + 리포트 | `python3 run_experiments.py --model main` |
| 긴 영상(4분대) 테스트 | `--bench-dataset activitynet --bench-longest` |

자세한 건 `영상_다운로드_URL.md`, `PREFLIGHT.md` 참고.
