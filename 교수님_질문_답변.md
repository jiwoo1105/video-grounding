# 교수님 질문 4개 — 공식 문서 기반 답변

모든 인용은 논문·GitHub·모델 카드 **원문 그대로**입니다.
제가 계산하거나 추론한 부분은 **[추론]** 으로 따로 표시했습니다.

---

# 1. 입력 영상의 길이·용량 제한이 있는가?

## 결론

**공식 문서 어디에도 "최대 몇 초까지" 라는 명시적 제한이 없습니다.**
대신 **세 개의 파라미터가 서로 물려서** 실질적 상한을 만듭니다.
**파일 용량(MB) 제한은 없습니다** — 길이와 해상도만 영향을 줍니다.

## 실질적 상한을 만드는 3가지

### ① `fps` — 초당 몇 프레임을 뽑을지

TimeLens 평가 스크립트 [`scripts/eval_timelens_bench.sh`](https://github.com/TencentARC/TimeLens/blob/main/scripts/eval_timelens_bench.sh):

```bash
#---------------------------- Configuration ----------------------------#
min_tokens=${min_tokens:-64}
total_tokens=${total_tokens:-14336}
FPS=${FPS:-2}
```

논문 부록 C.2 (arXiv 2512.14698v2):

> "**Model Configuration.** We sample video frames at 2 FPS."

### ② `total_tokens` — 영상 전체에 배정하는 비주얼 토큰 예산

같은 논문 부록 C.2:

> "For all ablation experiments, we set the minimum number of tokens per frame
> (i.e., every two merged frames) to min_tokens = 16, and the maximum number of
> tokens for the entire video to total_tokens = 3584. Under this configuration,
> **the model adaptively adjusts the spatial resolution based on the video's
> duration and raw resolution.** For the final TimeLens models presented in Tab. 1,
> we scale the resolution budget to min_tokens = 64 and total_tokens = 14336.
> **Under this setting, for a 110-second video, the maximum resolution per frame
> is about 320 × 320 pixels.**"

**이게 핵심 메커니즘입니다.** 영상이 길어지면 프레임 수가 늘고, 정해진 토큰 예산을
프레임 수로 나누므로 **프레임당 해상도가 자동으로 낮아집니다.** 길이 제한 대신
화질이 떨어지는 구조예요.

### ③ 프레임 수 상한 — 문서에 잘 안 드러난 부분

**TimeLens 평가 경로에는 프레임 상한 인자가 아예 없습니다.**
`evaluation/eval_dataloader.py` 의 인자는 셋뿐입니다:

```python
parser.add_argument("--min_tokens", type=int, default=16)
parser.add_argument("--total_tokens", type=int, default=3584)
parser.add_argument("--fps", type=int, default=2)
```

그래서 상위 라이브러리 기본값이 적용됩니다.
[`qwen_vl_utils/vision_process.py`](https://github.com/QwenLM/Qwen3-VL):

```python
FPS = 2.0
FRAME_FACTOR = 2
FPS_MIN_FRAMES = 4
FPS_MAX_FRAMES = 768
```

**[추론]** fps=2 × 768프레임 = **384초(6.4분)** 가 사실상의 분기점입니다.
그 이상은 오류가 나는 게 아니라 **프레임이 균등하게 솎아집니다**(실효 fps가 떨어짐).

한편 README 경로 문자열에 보이는 `maxframes-448` 은 하드코딩이 아니라
`filter_data_qwen3_vl.sh` 에서 **계산된 값**입니다:

```bash
fps_max_frames=$((total_tokens / min_tokens * 2))     # 14336 / 64 * 2 = 448
```

`* 2` 인 이유는 논문에 나옵니다 —
> "every two consecutive video frames are merged during the patch-embedding stage
> of the vision encoder"

**[추론]** 448프레임(**224초**)에서 토큰 예산이 소진되고, 그 뒤로는 프레임당 해상도가
`min_pixels` 바닥에 닿습니다. 768프레임(384초)부터는 프레임 자체가 솎아집니다.

## TimeLens2 는 훨씬 여유롭습니다

| | TimeLens | TimeLens2 |
|---|---|---|
| 토큰 예산 | 14,336 | **128,000** (모델 카드) |
| 프레임 상한 | 미노출 → 768 | 평가 `frames_limit=2048` |
| 학습 컨텍스트 | — | **100K 토큰** |

TimeLens2 [프로젝트 페이지](https://mcg-nju.github.io/TimeLens2/):
> "TimeLens2-93K and **contexts up to 100K tokens** teach one 4B or 8B model to
> retain sparse evidence through distractor-heavy videos."

논문 §3.1 (학습 데이터 수집):
> "we stratify 34,867 YouTube videos across five duration ranges,
> **from under one minute to over one hour**, and diverse visual domains."

프로젝트 페이지 통계: **평균 영상 길이 10.2분**.

**[추론]** TimeLens2 는 설계상 롱비디오를 겨냥했습니다. 긴 영상이면 TimeLens2 가 유리합니다.

## 명시적 길이 필터가 있는 유일한 곳 — 학습

`train_scripts/run_grpo_qwen3_8b.sh`:

```bash
--min_video_len 5 \
--max_video_len 500 \
```

**학습 때만** 5~500초로 거릅니다. **추론에는 이런 필터가 없습니다.**

## 정리

| 구간 | 어떻게 되나 |
|---|---|
| ~110초 | 프레임당 약 320×320 유지 (논문 명시) |
| ~224초 | 토큰 예산 소진, 해상도가 바닥에 닿기 시작 **[추론]** |
| ~384초 | 프레임 솎기 시작 (768프레임 상한) **[추론]** |
| 그 이상 | 오류는 없지만 실효 fps 하락 → 정확도 저하 |

**OOM 대응 가이드는 어느 레포에도 없습니다.** 다만 TimeLens 평가 코드에 이런
주석이 있어서, 긴 영상이 OOM 위험이라는 인식은 있습니다:

```python
# Sort by video length in descending order
# 1. balance the video length for each GPU
# 2. long videos are more likely to cause OOM, so we put them first
annos.sort(key=lambda x: x["duration"], reverse=True)
```

---

# 2. 우리 코드에서 프레임 수를 조절할 수 있는가?

## 결론: 됩니다. 인자 3개로 제어합니다.

```bash
python3 vtg_run.py --model timelens-8b --video a.mp4 --query "..." \
    --fps 2 --total-tokens 14336 --max-frames 448
```

| 인자 | 하는 일 | 기본값 |
|---|---|---|
| `--fps` | 초당 뽑을 프레임 수 | 2 (공식과 동일) |
| `--total-tokens` | 영상 전체 토큰 예산 | 모델별 공식값 |
| **`--max-frames`** | **프레임 수 상한** | 미지정 → 768 |

> `--max-frames` 는 교수님 질문을 받고 **오늘 추가했습니다.**
> 원래는 `fps` 와 `total_tokens` 만 노출돼 있었습니다.

## 셋의 관계

```
프레임 수    = min(영상길이 × fps,  max_frames,  768)
프레임당 토큰 = total_tokens / 프레임 수        (min_tokens 가 하한)
프레임당 해상도 = 프레임당 토큰 × (patch×2)²
```

**같은 30초 영상에서 fps 를 바꾸면:**

| fps | 프레임 | 프레임당 토큰 | 효과 |
|---|---|---|---|
| 1 | 30 | 478 | 성기게 보되 화질 높음 |
| **2** | **60** | **239** | **공식 기본값** |
| 4 | 120 | 119 | 촘촘히 보되 화질 낮음 |

**총 연산량은 `total_tokens` 로 거의 고정**되고, fps 는 그 예산을
**시간 해상도 ↔ 공간 해상도** 중 어디에 쓸지 정하는 손잡이입니다.

## 웹 UI 는 자동 결정

`app.py` 는 영상 길이를 재서 알아서 잡습니다 (사용자가 몰라도 되게):

```python
def budget_for(duration):
    if duration <= 60:  return 32768, 2.0
    if duration <= 180: return 16384, 2.0
    if duration <= 600: return 16384, 1.0
    return 8192, 1.0
```

## 참고 — TimeLens2 평가는 벤치마크마다 fps 를 다르게 씁니다

`evaluation/vlmeval/dataset/video_dataset_config.py`:

| 벤치마크 | fps |
|---|---|
| Charades / ActivityNet / QVHighlights | **4.0** |
| VUE-TR, VUE-TR-V2 | 1.0 |
| MomentSeeker, Ego4D-NLQ | 2.0 |

짧은 영상엔 fps 를 올리고 긴 영상엔 낮춥니다. 우리 `budget_for` 와 같은 발상입니다.

---

# 3. 벤치마크 3개는 어떤 데이터인가

TimeLens-Bench 는 기존 3대 데이터셋을 정제한 것입니다.

| | Charades-STA | ActivityNet Captions | QVHighlights |
|---|---|---|---|
| **영상 출처** | AMT 작업자가 **자기 집에서 직접 촬영** | 웹 영상 (ActivityNet) | **YouTube 브이로그 + 뉴스** |
| 분야 | 실내 일상 행동 | 개방 도메인 | 라이프스타일/여행/뉴스 |
| 원본 규모 | 13,898 train + 4,233 test 쌍 | 20k 영상 / 100k 문장 / 849시간 | 10,148 영상 / 10,310 질의 |
| 원본 평균 길이 | 30.1초 | **180초** | 150초 |
| 질의 평균 길이 | 6.3 단어 | 13.48 단어 | 11.3 단어 |
| **원본 주석 방식** | **반자동** (문장 분해+키워드 매칭 → 저자 2인 검수) | **수작업** (AMT 112명) | **수작업** (AMT 2단계) |
| **정제본** | 1,313영상 / 3,363주석 / 29.6초 | 1,455 / 4,500 / 134.9초 | 1,511 / 1,541 / 149.6초 |

## ① Charades-STA — "할리우드 인 홈즈"

**가장 특이한 데이터셋입니다.** 연구실에서 찍은 게 아니라 **크라우드워커들이
자기 집에서 대본을 받아 연기한 영상**입니다.

원논문 [Hollywood in Homes (ECCV 2016)](https://arxiv.org/abs/1604.01753) 초록:

> "Instead of shooting videos in the lab, we ensure diversity by distributing and
> crowdsourcing the whole process of video creation from script writing to video
> recording and annotation. Following this procedure we collect a new dataset,
> Charades, with **hundreds of people recording videos in their own homes**,
> acting out casual everyday activities."

> "we take the Hollywood filming process to the homes of hundreds of people on
> Amazon Mechanical Turk (AMT). AMT workers follow the three steps of filming
> process: (1) script generation; (2) video direction and acting based on scripts;
> and (3) video verification"

실제 질의 (정제본 `charades-timelens.json` 원문):

```
A woman is repeatedly flipping the switch on the wall.        [25, 30]
A woman is eating chips.                                      [1, 24]
A woman holding chips leans against the door frame.           [0, 1]
The woman stands up straight from leaning on the door frame.  [23, 24]
a person is putting a picture onto the wall.                  [5, 30]
A person puts a bag on the table.                             [6, 7]
person runs up the stairs.                                    [3, 8]
A person closes the wardrobe door.                            [27, 29]
```

**우리가 쓴 2Y8XQ.mp4 가 바로 이 데이터셋입니다** (화장실에서 물 마시는 30초 영상).

### ★ Charades-**STA** 의 시간 라벨은 사람이 안 찍었습니다

원본 Charades 에는 "행동 라벨 + 시간"만 있고 **문장 질의가 없었습니다.**
Gao et al. 이 그걸 **자동으로 문장에 붙였습니다.** TALL 논문 §4 원문:

> "we designed a **semi-automatic** way to generate sentence temporal annotation.
> The first step is **sentence decomposition**: a long sentence is split to sub-sentences
> by a set of conjunctions (which are collected by hand)... The second step is
> **keyword matching**: we extract keywords for each activity categories and match them
> to sub-sentences, if they are matched, the temporal annotation (start and end time)
> are assigned to the sub-sentences. The third step is a **human check**: for each pair
> of sub-sentence and temporal annotation, **we (two of the co-authors)** checked whether
> the sentence made sense and whether they matched the activity annotation."

**[추론] 이게 TimeLens 가 Charades-STA 에서 34.9% 오류를 찾아낸 이유입니다.**
시간 경계가 문장이 아니라 **원래 행동 라벨**에 맞춰져 있고, 검수도 크라우드가 아니라
**저자 2명**이 했습니다. 정제 벤치마크가 필요했던 근본 원인이에요.

**링크**
- 논문: <https://arxiv.org/abs/1705.02101> (TALL, ICCV 2017)
- 주석/코드: <https://github.com/jiyanggao/TALL>
- 영상: <https://prior.allenai.org/projects/charades>

## ② ActivityNet Captions — 개방 도메인, 연쇄 사건

[논문](https://arxiv.org/abs/1705.00754) (ICCV 2017) §4.1:

> "On average, each of the 20k videos in ActivityNet Captions contains
> **3.65 temporally localized sentences**, resulting in a total of 100k sentences...
> Each sentence has an average length of 13.48 words."

> "On average, each sentence describes 36 seconds and 31% of their respective
> videos... we found that **10% of the temporal descriptions overlap**"

실제 질의 (정제본 원문):

```
A surfer catches a wave and rides it, while another surfer rides the same
wave in the opposite direction...                                    [0, 22]
A group of people are preparing for a bicycle race.                  [0, 19]
Those people ride bicycles speeding on the track until the finish line. [20, 49]
A boy is talking to a camera.                                        [0, 3]
He goes into a bathroom and drinks a cup of mouthwash.               [4, 43]
He spits it out into the sink in front of him.                       [44, 45]
```

**주목할 점**: `He`, `Those people` 같은 **대명사**가 있습니다. 작업자가 영상 전체를
**문단으로 먼저 쓰고** 각 문장에 시간을 붙였기 때문이에요. 그래서 질의 하나만
떼어놓으면 무슨 말인지 모릅니다. **[추론]** 이 구조 때문에 모델이 "질의를 이해"하지
않고 "영상을 균등 분할"만 해도 점수가 나올 수 있습니다.

### 주석 방식 — AMT 수작업, "문단 먼저" 2단계

논문 §7.3 원문:

> "We used **Amazon Mechanical Turk** to annotate all our videos. Each annotation task was
> divided into two steps: (1) **Writing a paragraph** describing all major events happening
> in the videos... and (2) **Labeling the start and end time** in the video in which each
> sentence in the paragraph event occurred. We find complementary evidence that workers are
> more consistent with their video segments and paragraph descriptions if they are asked to
> annotate visual media... using natural language first. Therefore, instead of asking workers
> to segment the video first and then write individual sentences, **we asked them to write
> paragraph descriptions first.**"

> "They were allowed to use **co-referencing words (ex, he, she, etc.)** to refer to subjects
> introduced in previous sentences."

> "In total, we had **112 workers** who annotated all our videos."

→ 위에서 본 `He goes into a bathroom...` 같은 대명사 질의가 **의도된 설계**임이 확인됩니다.

**링크**
- 논문: <https://arxiv.org/abs/1705.00754>
- 데이터: <https://cs.stanford.edu/people/ranjaykrishna/densevid/>

## ③ QVHighlights — 유튜브 브이로그·뉴스

[논문](https://arxiv.org/abs/2107.09609) (NeurIPS 2021) 초록:

> "It consists of over 10,000 YouTube videos, covering a wide range of topics,
> from everyday activities and travel in **lifestyle vlog** videos to social and
> political activities in **news** videos."

§3.1:
> "These raw videos are then segmented into **150-second short videos** for annotation."

실제 질의 (정제본 원문):

```
Someone opens a cup of instant noodles and adds the seasoning packet
and hot water.                                                       [78, 91]
A woman wearing a plaid shirt is holding a bottle of cosmetics and
talking, during which she uses her left hand to brush her hair back... [73, 132]
Liverpool player is interviewed next to a logo wall.
Rams and moose are on the side of the road.
A woman in a black dress giving a tour of her hotel room.
People butcher meat from a carcass.
```

Charades 와 비교하면 **훨씬 시각적으로 구체적**입니다 — "빨간 상의", "체크 셔츠"
처럼 외형 묘사가 들어갑니다.

**특징**: 원본은 **질의 하나에 여러 개의 떨어진 구간**을 허용합니다 (평균 1.8개).

> "different from previous work where only a single moment can be selected for a
> query-video pair, **users can select multiple disjoint moments in our setup**."

**링크**
- 논문: <https://arxiv.org/abs/2107.09609>
- 코드/주석: <https://github.com/jayleicn/moment_detr>

---

# 4. 벤치마크를 어떻게 구성했는가 — 사람이 일일이 봤는가?

## 결론: **네, 사람이 영상을 하나하나 보고 손수 고쳤습니다.**

TimeLens-Bench [데이터셋 카드](https://huggingface.co/datasets/TencentARC/TimeLens-Bench):

> "During our annotation process, we identified critical quality issues within
> existing datasets and **performed extensive manual corrections**. We observed a
> **dramatic re-ranking of models** on TimeLens-Bench compared to legacy benchmarks,
> demonstrating that TimeLens-Bench provides more reliable evaluation."

## 작업 방식 — 논문 §3.2 원문

> "Our pipeline follows a **diagnose-then-refine** workflow. Given a video-query pair
> from existing datasets, **annotators first carefully review the video** to identify
> potential errors against the criteria in Sec. 3.1. If an error is detected, they
> select the error category, then either revise the query or choose a new valid event
> to describe. Subsequently, the precise temporal segment is annotated.
> The core principle is that **the same annotator performs both error detection and
> subsequent correction**, which not only improves efficiency but also strengthens
> annotators' awareness of potential errors."

**중요한 디테일** — 원래 정답을 안 보여줬습니다:

> "**During the process, we do not provide original temporal segments to annotators.**"

**[추론]** 기존 라벨에 끌려가지 않게 하려는 조치입니다. 처음부터 다시 매긴 셈이에요.

## 품질 관리 — 교차 검증 + 배치 반려

> "Upon completion of each small data batch, **every sample is assigned to a different
> annotator for cross-validation** and error correction. **If the error rate in a batch
> exceeds a threshold, the entire batch is rejected for re-annotation** and then
> validated again."

> "For annotator selection and training, we sampled a small subset of data for trial
> annotation with **over a dozen vendors**, then selected the vendor with the highest
> quality and consistency. Before formal annotation, we provided a detailed handbook
> and conducted several training sessions."

## 얼마나 고쳤나 (논문 Table 4)

| 데이터셋 | 주석 수 | **질의 재작성** | **시간 수정** |
|---|---|---|---|
| Charades-TimeLens | 3,363 | 2,467 | 896 |
| ActivityNet-TimeLens | 4,500 | 3,137 | 1,363 |
| QVHighlights-TimeLens | 1,541 | 859 | 682 |
| **합계** | **9,404** | **6,463 (69%)** | **2,941** |

**질의의 69%를 다시 썼습니다.** 시간 경계 수정보다 **질문 자체를 갈아엎은 게 2배 이상** 많아요.

> "For each source dataset, annotations with high-quality queries had their
> corresponding temporal segments refined. For the remaining annotations with
> low-quality queries, we either revised the queries or rewrote them entirely.
> **A small fraction of queries that were deemed unfixable were subsequently discarded.**"

## 왜 이 작업이 필요했나 — 오류율

논문 §3.3 Finding 1:
> "**Finding 1: Widely-used benchmarks have an alarmingly high proportion of errors.**"

> "For example, in Charades-STA, we find that **20.6% of samples violate query
> uniqueness, while 34.9% exhibit annotation accuracy issues.**"

오류 유형 5가지 (Fig. 3):
> "multiple event occurrences, no event occurrence, duplicate queries for the same
> video, unclear query, and inaccurate annotation"

## 원본 3종은 어떻게 만들었나 (비교)

| | 방식 | 검수 |
|---|---|---|
| Charades **원본** | AMT 크라우드가 자기 집에서 촬영 + 라벨 | 3단계 (대본→촬영→검증) |
| Charades-**STA** | **반자동** — 문장 분해 + 키워드 매칭 | **저자 2명** |
| ActivityNet Cap. | AMT 수작업, 문단→타임스탬프 2단계 | 작업자 112명 |
| QVHighlights | AMT 수작업, 다중 구간 + saliency 점수 | 2단계 |
| **TimeLens-Bench** | **AMT 아님 — 전문 벤더, 원본 정답 미공개** | **배치별 교차검증 + 반려** |

**[추론]** TimeLens 가 유독 엄격한 이유는 위 3종 중 **Charades-STA 만 반자동**이고
나머지 둘도 "빠르게 많이" 쪽에 최적화돼 있었기 때문입니다.

## ★ 학습 데이터는 다릅니다 — 자동 생성

**평가셋과 학습셋을 완전히 분리했습니다.**

| | 방식 |
|---|---|
| **평가 (TimeLens-Bench)** | **사람이 수작업** |
| **학습 (TimeLens-100K)** | **Gemini-2.5-Pro 자동 생성** |

논문 §3.4:
> "Given the vast scale of the training sets, we employ an **automated pipeline**...
> Owing to the poor quality of these training datasets... **we re-annotate the videos
> rather than refining existing labels.**"

부록 F:
> "Specifically, we use **Gemini-2.5-Pro, currently the best-performing VTG model**,
> for re-annotation."

그리고 편향 방지에 대해:
> "Notably, our automated re-annotation for training data is developed **entirely
> independently of the manual benchmark refinement process, ensuring an unbiased
> evaluation.**"

**[추론] 교수님이 파고들 수 있는 지점**: 학습 데이터를 만든 Gemini-2.5-Pro 가
정제 벤치마크에서도 **1위(평균 mIoU 60.4)** 입니다. 즉 TimeLens 는 Gemini 를
증류(distillation)한 셈이고, 실제로 TimeLens-8B(58.0)는 Gemini-2.5-Pro 를 못 넘습니다.
논문도 "surpassing GPT-5 and Gemini-2.5-**Flash**" 라고만 하고 Pro 는 언급하지 않습니다.

다만 **평가 벤치마크 자체는 사람이 만들었으므로** "Gemini에 유리한 자를 썼다"는
비판은 성립하지 않습니다. 한계는 "학생이 선생을 넘기 어렵다" 쪽입니다.

---

# 출처 목록

| 자료 | 링크 |
|---|---|
| TimeLens 논문 (CVPR 2026) | <https://arxiv.org/abs/2512.14698> |
| TimeLens 코드 | <https://github.com/TencentARC/TimeLens> |
| TimeLens-Bench | <https://huggingface.co/datasets/TencentARC/TimeLens-Bench> |
| TimeLens2 논문 | <https://arxiv.org/abs/2607.17423> |
| TimeLens2 코드 | <https://github.com/MCG-NJU/TimeLens2> |
| TimeLens2 프로젝트 | <https://mcg-nju.github.io/TimeLens2/> |
| Charades (Hollywood in Homes) | <https://arxiv.org/abs/1604.01753> |
| Charades-STA (TALL) | <https://arxiv.org/abs/1705.02101> |
| ActivityNet Captions | <https://arxiv.org/abs/1705.00754> |
| QVHighlights | <https://arxiv.org/abs/2107.09609> |
| qwen-vl-utils | <https://github.com/QwenLM/Qwen3-VL> |

> 3·4번의 모든 수치는 원논문 PDF 를 직접 열어 **한 문장씩 대조 확인**했습니다.
> 대조 과정에서 흔히 인용되는 "ActivityNet 평균 2분" 이 틀렸고 논문 값은
> **180초**(§7.1, Table 4)임을 확인해 수정했습니다.
