# 인스턴스 재구축 가이드 — 처음부터 끝까지

인스턴스를 삭제한 뒤 다시 만들 때 **이 문서만 따라가면 됩니다.**
코드와 영상은 GitHub에 있으니 잃어버릴 게 없습니다.

> 레포: <https://github.com/jiwoo1105/video-grounding>

**전체 소요: 약 25분 / 비용 약 ₩700**

---

# 1. 인스턴스 생성

엘리스 콘솔 → 런박스 → 인스턴스 생성

| 항목 | 값 |
|---|---|
| GPU | **G-NAHPM-40** (A100 80GB PCIe MIG 3g-40GB) — ₩1,380/시간 |
| 스토리지 | **128 GiB** — ₩20.48/시간 |
| 실행 환경 | **VSCode (CUDA 12.8)** |

**실행 환경 주의**

| 선택지 | 판단 |
|---|---|
| **VSCode (CUDA 12.8)** | ✅ 이걸 고르세요 |
| VSCode (CUDA 11.8) | ❌ torch cu128 이 안 돌아감 |
| 버전 없는 VSCode / Jupyter | ⚠️ CUDA 버전 불명확 |
| SSH-Only | ⚠️ 되지만 키 발급이 번거로움 |

생성되면 브라우저에서 VSCode 가 열립니다.

---

# 2. 코드 받기 (1분)

`Terminal` → `New Terminal` 에서:

```bash
cd ~
git clone https://github.com/jiwoo1105/video-grounding.git Video_grounding
cd Video_grounding
ls
```

이렇게 보이면 성공입니다:

```
app.py  vtg_run.py  run_experiments.py  setup_env.sh  videos/  ...
```

**영상(2.2MB)도 레포에 들어 있어서** 따로 받을 필요 없습니다.

> VSCode 왼쪽 탐색기에 폴더가 안 보여도 상관없습니다. 터미널만으로 다 됩니다.
> 편집기에서 보고 싶으면 `File` → `Open Folder` → `/home/elicer/Video_grounding`

---

# 3. 환경 구성 (15분)

```bash
bash setup_env.sh
```

스크립트가 순서대로 하는 일:

| 단계 | 내용 |
|---|---|
| [0/5] | GPU / 드라이버 확인 |
| [1/5] | `~/vtg-env` 가상환경 생성 |
| [2/5] | PyTorch 설치 (드라이버 버전 보고 cu126/cu128 자동 선택) |
| [3/5] | transformers, qwen-vl-utils, gradio 등 |
| [4/5] | **GPU 커널 실제 동작 검증** |
| [5/5] | 모델 미리 받기 (선택) |

**중간에 "지금 다운로드할까요? [y/N]"** 이 뜨면 **`y`** — 모델 26GB, 약 10분.

마지막에 이게 뜨면 성공입니다:

```
compute cap  : 8.0
arch list    : ['sm_75', 'sm_80', 'sm_86', 'sm_90', ...]
bf16 matmul  : OK
>>> 환경 정상 <<<
```

`arch list` 에 **`sm_80`** 이 있어야 합니다 (A100 이 sm_80).

그다음:

```bash
source ~/vtg-env/bin/activate
sudo apt-get install -y ffmpeg      # 잘라낸 클립 재생용
```

> **터미널을 새로 열 때마다 `source ~/vtg-env/bin/activate` 를 다시 해야 합니다.**

---

# 4. 동작 확인 (1분)

가장 가벼운 모델로 먼저 확인합니다.

```bash
python3 vtg_run.py --model timelens2-4b --video videos/2Y8XQ.mp4 \
    --query "A man drinks water with a glass"
```

이런 출력이 나오면 정상입니다:

```
[load] timelens2-4b  (MCG-NJU/TimeLens2-4B)
[load] 8.0s / VRAM 8.3 GB
  [[16.0, 20.0]]    4.19s  10.9GB | A man drinks water with a glass
```

**16~20초** 근처가 나오면 맞은 겁니다 (실제 물 마시는 구간은 17~19초).

---

# 5. 웹 UI 실행

```bash
python3 app.py --share
```

터미널에 공개 링크가 뜹니다:

```
Running on public URL: https://xxxxxxxx.gradio.live
```

**`--share` 를 쓰는 이유** — VSCode 포트 포워딩을 거치면 Gradio 의 CSS 가 안 불러와져서
화면이 깨집니다. `--share` 는 Gradio 자체 터널이라 정상적으로 보입니다.

포트 포워딩으로도 되긴 합니다 (`python3 app.py` → 하단 `PORTS` 탭 → 7860).

---

# 6. UI 사용법

```
① "서버에 있는 영상" 드롭다운 → videos/2Y8XQ.mp4
② 영상이 재생됩니다
③ "준비된 질의" 드롭다운에서 문장 선택 (또는 직접 입력)
④ 모델 선택
⑤ [장면 찾기]
```

**나오는 것**

| | |
|---|---|
| 결과 | `16.00초 ~ 20.00초 (길이 4.00초)` |
| 타임라인 | 영상 전체에서 어디인지 막대로 |
| 찾은 구간 | 그 부분만 잘라서 재생 (ffmpeg 필요) |
| 모델 원본 출력 | 접혀 있음 |

**내 영상을 쓰려면** 드롭다운을 `— 직접 올리기 —` 로 두고 영상 칸에 끌어다 놓으면 됩니다.

---

# 7. 모델 4종

| UI 이름 | 저장소 | 크기 | 특징 |
|---|---|---|---|
| **TimeLens-8B** | `TencentARC/TimeLens-8B` | 17GB | CVPR'26. 기본값 |
| TimeLens2-8B | `MCG-NJU/TimeLens2-8B` | 17GB | 최신. 다중 구간 출력 |
| TimeLens2-4B | `MCG-NJU/TimeLens2-4B` | 9GB | 가볍고 빠름 |
| TimeLens-7B | `TencentARC/TimeLens-7B` | 16GB | Qwen2.5-VL 계열 |

`setup_env.sh` 에서 `y` 를 누르면 **TimeLens-8B + TimeLens2-4B** 를 미리 받습니다.
나머지 둘은 UI 에서 처음 고를 때 자동으로 받습니다 (몇 분 소요).

**발표 전에 쓸 모델을 한 번씩 미리 돌려두세요.** 시연 중에 다운로드로 멈추면 곤란합니다.

---

# 8. 끝낼 때

```bash
# 결과 파일이 있으면 먼저 내려받기
# VSCode 탐색기에서 results/ 우클릭 → Download
```

| 동작 | GPU 요금 | 스토리지 요금 | 모델 캐시 |
|---|---|---|---|
| **종료(정지)** | ❌ | ⚠️ **계속 나감** (약 ₩6/시간) | ✅ 유지 |
| **삭제** | ❌ | ❌ | ❌ 사라짐 |

- **며칠 안에 다시 씀** → 종료만
- **한동안 안 씀** → 삭제 (이 문서로 25분이면 재구축)

---

# 자주 나오는 문제

| 증상 | 원인 / 해결 |
|---|---|
| `GPU(CUDA)를 찾을 수 없습니다` | 맥북에서 실행 중. 인스턴스 터미널에서 하세요 |
| `ModuleNotFoundError: No module named 'av'` | `source ~/vtg-env/bin/activate` 안 함 |
| `No such file or directory: ~/vtg-env/bin/activate` | `setup_env.sh` 가 중간에 죽음. 출력 확인 |
| `no kernel image is available` | CUDA 12.8 이미지가 맞는지 확인 후 재실행 |
| UI 글자만 나오고 스타일이 깨짐 | `python3 app.py --share` 로 실행 |
| 클립 칸이 안 보임 | `sudo apt-get install -y ffmpeg` |
| `CUDA out of memory` | 모델을 TimeLens2-4B 로 |
| `구간을 찾지 못했습니다` | 질의가 모호함. 더 구체적으로 |
| git clone 이 안 됨 | 레포가 Public 인지 확인 |

---

# 참고 — 이 프로젝트가 뭔지

**Video Temporal Grounding**: 영상 + 자연어 문장 → 그 사건이 일어난 시간 구간을 찾는 태스크.

| 문서 | 내용 |
|---|---|
| `Qwen_모델_정리.md` | 논문 분석, 왜 특화 모델이 필요한지 |
| `2Y8XQ_분석.md` | 데모 영상 내용 + 질의 + 발표 시나리오 |
| `질의_예시.md` | 좋은 질의 문장 쓰는 법 |
| `영상_다운로드_URL.md` | 벤치마크 데이터 받는 곳 |

**핵심 논문**

- [TimeLens](https://github.com/TencentARC/TimeLens) (CVPR 2026)
- [TimeLens2](https://github.com/MCG-NJU/TimeLens2) (arXiv 2026-07)
